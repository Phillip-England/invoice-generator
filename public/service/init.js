//this function is automatically fired upon loading the page

const init = () => {
    
    //getting our main root container
    const root = document.getElementById('root_container')
    root.style.height = window.innerHeight + 'px'

}

init()